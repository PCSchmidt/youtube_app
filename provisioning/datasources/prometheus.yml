# Declarative Prometheus datasource for Grafana (auto-provisioned).
apiVersion: 1

datasources:
  - name: Prometheus
    uid: yt-rag-prom
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
